// Вправа 2: Передача об'єкта за посиланням

// 2. Функція inc з об'єктом (передача за посиланням)
function inc(num) {
  num.n++;
}

// Приклад виклику
const obj = { n: 5 };
inc(obj);
console.dir(obj); // Виведе: { n: 6 }
