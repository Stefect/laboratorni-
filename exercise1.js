// Вправа 1: Скалярні типи та посилання

// 1. Функція inc з числовим параметром (передача за значенням)
function inc(n) {
  return n + 1;
}

// Приклад виклику
const a = 5;
const b = inc(a);
console.dir({ a, b });
